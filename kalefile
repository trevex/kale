project("simple")

kubectl = require("kubectl", { "version": ">=v1.10.0" })

def deploy(params):
    print(params)
    kubectl.apply("test")
    print("last")

target(deploy, params = {
    "values": schema.filename(required = True)
})
