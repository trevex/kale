project("simple")

kubectl = require("kubectl", { "version": ">=v1.10.0" })

def build(params):
    print(params)
    kubectl.apply("test")
    print("last")

target(build, params = {
    "values": schema.filename(required = True)
})
